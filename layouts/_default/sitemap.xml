{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  {{- if and .Permalink (not .Params.noIndex) (not (eq .Layout "search")) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05+08:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- else }}
    {{- if .IsHome }}
    <changefreq>daily</changefreq>
    {{- else if (eq .Section "posts") }}
    <changefreq>weekly</changefreq>
    {{- else }}
    <changefreq>monthly</changefreq>
    {{- end }}
    {{- end }}
    {{- if .Params.sitemap.priority }}
    <priority>{{ .Params.sitemap.priority }}</priority>
    {{- else }}
    {{- if .IsHome }}
    <priority>1.0</priority>
    {{- else if (eq .Section "posts") }}
    <priority>0.8</priority>
    {{- else if .IsPage }}
    <priority>0.6</priority>
    {{- else }}
    <priority>0.4</priority>
    {{- end }}
    {{- end }}
    {{- range .Translations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}"/>
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
